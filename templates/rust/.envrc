use flake

# shared template hooks provided by the dev shell (update reminder, ...);
# no-op when the shell doesn't provide any
if [ -n "${TEMPLATE_DIRENV_HOOK:-}" ] && [ -f "$TEMPLATE_DIRENV_HOOK" ]; then
  source "$TEMPLATE_DIRENV_HOOK"
fi
